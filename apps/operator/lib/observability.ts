export function emitMetric(name: string, value: number, tags?: Record<string, any>) {
  // No-op for now; add integration as needed
}

export function emitLog(event: string, data?: Record<string, any>) {
  // No-op for now; add integration as needed
}
